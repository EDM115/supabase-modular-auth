# Frontend Environment Variables
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Optional same-origin proxy target (server-side rewrite)
# When set, frontend can call /auth/* directly and cookies stay first-party.
# FRONTEND_PROXY_TARGET=http://localhost:3000
